# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Simple Node.js (CommonJS) Hello World project. Single-file application with no external dependencies.

## Commands

- **Run:** `npm start` (executes `node hello.js`)
- **Tests:** No test framework configured yet
- **Lint:** No linter configured yet
